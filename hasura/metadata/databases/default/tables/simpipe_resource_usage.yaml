table:
  schema: simpipe
  name: resource_usage
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - step_id
        - cpu
        - memory
        - memory_max
        - rx_value
        - tx_value
        - time
      filter: {}
