#  remote schema is not needed as the graphql server is exposed directly

# - name: SIM-PIPE-backend
#   definition:
#     url_from_env: REMOTE_SCHEMA_URL
#     timeout_seconds: 60
#     forward_client_headers: true
#   comment: ""
